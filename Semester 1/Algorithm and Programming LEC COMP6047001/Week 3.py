Topic for today:
    1.  While loop
        syntax:
            while <test_expr> :
                <body>
        finite and infinite loops, break, continue, pass
    2.  Lists (mutable) --> []
        Creating a list
        Indexing
        Length of list
        Assignment
        Some useful functions : append(), extend(), insert(index,val),
                                remove(value or index), min(), max(),
                                sum(), sort(), islower(), isupper()
    3.  for loop
        syntax:
            for <item> in <iterable>:
                <body>
    4.  range([start], stop, [increment])